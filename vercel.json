{{"buildCommand":"cd FRONTEND && npm install && npm run build","outputDirectory":"FRONTEND/.next"}

  "buildCommand": "cd FRONTEND && npm install && npm run build",
  "outputDirectory": "FRONTEND/.next"
}
